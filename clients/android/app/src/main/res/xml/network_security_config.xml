<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Android XML cannot express private IP ranges. Rivune validates every
         server and resolved media URL before allowing cleartext transport. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
