name: Continuous integration

on:
  pull_request:
  push:
    branches:
      - main
  merge_group:

permissions:
  contents: read
  actions: read
  packages: read

jobs:
  gate:
    name: Complete project gate
    uses: ./.github/workflows/release-gate.yml
